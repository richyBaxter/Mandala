# secrets / credentials — never commit these
.env
.env.*
*.pem
*.key
*.p12
secrets.*
*.secret
credentials*.json
.netrc
*token*.txt

# AI prompt / instruction files — keep private
prompts/
*.prompt
*.prompt.md
*.prompt.txt
.claude/
CLAUDE.md
AGENTS.md

# CI-generated artefacts (built fresh each deploy)
prompt.txt
commentary.json
news.json
etf_flows.json
tools/_metrics.json

# python / os cruft
__pycache__/
*.pyc
.DS_Store
.venv/
node_modules/
